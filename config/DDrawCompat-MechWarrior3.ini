# DDrawCompat config for MechWarrior 3
# MW3's game clock is frame-rate-coupled; cap to 30 fps to normalize game speed
# (matches the previously-working dgVoodoo FPSLimit=30).
FpsLimiter = flipstart(30)
